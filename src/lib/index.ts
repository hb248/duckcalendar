// place files you want to import through the `$lib` alias in this folder.

// lib/index.ts

export * from './api'; // Exportiert alle Funktionen und Klassen aus api.ts
export * from './types'; // Exportiert alle Typen aus types.ts





// Keine Ahnung wie das file entstanden ist, aber in den zwei files wird sowieso alles exportiert, also whatever. idc anymore